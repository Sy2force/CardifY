services:
  - type: web
    name: cardify-backend-new
    env: node
    plan: free
    rootDir: ./app/backend
    buildCommand: npm install && npm run build
    startCommand: node dist/server.js
    # Force deployment with latest fixes - Nov 8, 2024
