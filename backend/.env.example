# ==========================================
# 🚀 CARDIFY BACKEND ENVIRONMENT VARIABLES
# ==========================================

# Server Configuration
# Port for local development (Render uses process.env.PORT)
PORT=8080

# Environment (development|staging|production)
NODE_ENV=development

# ==========================================
# 🗄️  DATABASE CONFIGURATION
# ==========================================

# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority&authSource=admin
MONGO_URI=mongodb+srv://cardifyuser:your_password@cluster.mongodb.net/cardify?retryWrites=true&w=majority&authSource=admin

# ==========================================
# 🔐 AUTHENTICATION & SECURITY
# ==========================================

# JWT Secret Key (CRITICAL: Use strong random string in production)
# Generate with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# JWT Token Expiration
JWT_EXPIRES_IN=7d

# ==========================================
# 🌐 FRONTEND & CORS CONFIGURATION
# ==========================================

# Frontend Application URL
CLIENT_URL=http://localhost:3008

# CORS Allowed Origins (comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:3008,http://localhost:3000

# ==========================================
# 📊 MONITORING & LOGGING
# ==========================================

# Log Level (error|warn|info|debug)
LOG_LEVEL=info

# Enable Request Logging
ENABLE_REQUEST_LOGS=true

# ==========================================
# 🔧 PRODUCTION OPTIMIZATIONS
# ==========================================

# API Rate Limiting (requests per window)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# Request Body Size Limit
BODY_LIMIT=10mb

# ==========================================
# 📝 RENDER DEPLOYMENT
# ==========================================
# 
# 🔸 Required for Render deployment:
#   NODE_ENV=production
#   PORT=10000 (auto-assigned by Render)
#   MONGO_URI=mongodb+srv://...
#   JWT_SECRET=64-char-random-string
#   CLIENT_URL=https://your-frontend.vercel.app
#
# 🔸 Security checklist:
#   ✅ Strong JWT_SECRET (64+ chars)
#   ✅ HTTPS URLs in production
#   ✅ MongoDB IP whitelist configured (0.0.0.0/0)
#   ✅ Environment variables secured in Render dashboard
#
# ==========================================
